{
  "name": "utilify",
  "version": "1.0.0",
  "type": "module",
  "description": "Premium text formatter for social media (Unicode) and rich text editors (HTML).",
  "main": "index.html",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/minify-html.cjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  },
  "dependencies": {
    "jsdom": "^28.1.0"
  }
}
