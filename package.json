{
  "name": "utilify",
  "version": "1.0.0",
  "type": "module",
  "description": "Premium text formatter for social media (Unicode) and rich text editors (HTML).",
  "main": "index.html",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/update-links-helper.cjs && node scripts/generate-sitemap-helper.cjs && vite build && node scripts/minify-html.cjs",
    "preview": "vite preview",
    "update-links": "node scripts/update-links-helper.cjs",
    "generate-sitemap": "node scripts/generate-sitemap-helper.cjs"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  },
  "dependencies": {
    "jsdom": "^28.1.0"
  }
}
